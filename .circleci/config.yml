version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout
      - run:
          name: Check the Stream Editor
          command: which sed
      - run:
          name: Update
          command: apt-get update
      - run:
          name: Install Autoconf
          command: yum autoconf
      - run:
          name: Install Automake
          command: yum automake
      - run:
          name: Install Libtool
          command: yum libtool